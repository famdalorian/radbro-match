{
  "name": "radbro-match-full",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run start --prefix backend\" \"npm run start --prefix radbro-match\"",
    "server": "npm start --prefix backend",
    "client": "npm start --prefix radbro-match",
    "install-all": "npm install && npm install --prefix radbro-match && npm install --prefix backend"
  },
  "dependencies": {
    "concurrently": "^8.2.0",
    "dotenv": "^16.4.7"
  }
}
